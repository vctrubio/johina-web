/** @type {import('next').NextConfig} */
const nextConfig = {
  // ... your other configurations
  images: {
    domains: ['source.unsplash.com'],
  },
  // ... more configurations if any
}

module.exports = nextConfig
